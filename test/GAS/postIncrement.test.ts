import main from '../base'; // It must be imported

main("`++i` costs less gas than `i++`, especially when it's used in `for`-loops (`--i`/`i--` too)", 'postIncrement.sol'); // Title of the issue and the fileneme to test
